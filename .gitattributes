* text=auto eol=lf

# The bootstrap 0.1.0 npm artifact was packed from these reviewed CRLF files.
# Keep their checkout bytes stable on every runner so the tagged source can
# reproduce the immutable registry tarball without weakening identity checks.
README.md text eol=crlf
package.json text eol=crlf
examples/crawler-handoff.mjs text eol=crlf
examples/productloop-capability-snapshot.mjs text eol=crlf
examples/qarinah-recorder.mjs text eol=crlf
