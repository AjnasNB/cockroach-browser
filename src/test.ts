/**
 * The complete Playwright Test runner and assertion surface.
 *
 * This is an unrestricted operator API. It intentionally preserves the
 * installed upstream contracts for fixtures, projects, reporters, retries,
 * assertions, parallelism, snapshots, and browser projects.
 */
export * from "@playwright/test";

